server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/{your-repo}/config-repo
          default-label: main
          search-paths: '{application}'

management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh
