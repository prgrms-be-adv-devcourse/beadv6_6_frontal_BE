spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI}
          default-label: ${CONFIG_REPO_LABEL:main}
          search-paths: ${CONFIG_REPO_SEARCH_PATHS:{application}}
